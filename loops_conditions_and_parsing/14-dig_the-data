#!/bin/bash

# Check if the log file is provided as an argument
if [ $# -ne 1 ]; then
    echo "Usage: $0 <logfile>"
    exit 1
fi

logfile=$1

awk '
{
    count[$1" "$9]++;
}
END {
    for (entry in count) {
        print count[entry], entry;
    }
}
' "$logfile" | sort -nr

